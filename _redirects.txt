# ── Protección del panel interno ──────────────────────────────
# Bloquea acceso directo a /interno/ desde bots y crawlers
# El panel tiene su propio login con Firebase Auth, pero esto
# evita que Netlify sirva los archivos a bots no autenticados

# Archivos estáticos del panel — solo accesibles desde el dominio propio
/interno/*  /interno/:splat  200

# Bloquear test-gemini en producción (solo para desarrollo)
/interno/test-gemini.html  /interno/index.html  302

# ── Redirects generales ───────────────────────────────────────
# www → sin www
https://www.casaverdecanas.com.br/*  https://casaverdecanas.com.br/:splat  301!
